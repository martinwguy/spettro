------------------------------------------------------------------------
If the evas_close functions are called on exit, it dumps core.
------------------------------------------------------------------------
Make playing multiple audio files work
------------------------------------------------------------------------
Add sox's heatmap
------------------------------------------------------------------------
Add optional frequency scale in hertz on the left and as notes on the right
------------------------------------------------------------------------
Add optional time axis at top and/or bottom
------------------------------------------------------------------------
Use multiple CPUs by multiple calc threads. Give each a bigger step
and assume that they run at the same speed? -> interesting effects on
startup if CPU availability is not even. Compensate?
------------------------------------------------------------------------
Fullscreen -f would be nice.
------------------------------------------------------------------------
Add OCTAVES & PPOCT determining graphic height like -w -h?
------------------------------------------------------------------------
Add pentagram staves and piano lines
preferably with subpixel positioning / antialiasing.
------------------------------------------------------------------------
Add GUI keystrokes to:
- pan up and down the frequency axis
- zoom in and out, which affects both the frequency and time scales equally
  (or is it better to zoom the frequency and time axes separately?)
- change brightness/contrast (i.e. change min_db, as max is normalised)
- change fftfreq (impacting the result cache)
------------------------------------------------------------------------
Green line flickers when enlarging with scarse processor
------------------------------------------------------------------------
Add "1" button to set 1:1 size
------------------------------------------------------------------------
Spettro window is always on top.
------------------------------------------------------------------------
If you jump right while it's still calculating, it should calculate for
the current screenful (from the playing position forwards or L-to-R?).
In general, limited result precalculation/caching would be better.
The visible region plus a screenful ahead?
------------------------------------------------------------------------
When enlarging and missing timer interrupts so it should scroll by >1,
the visuals get out of sync with the audio.
------------------------------------------------------------------------
